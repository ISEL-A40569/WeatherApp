<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_notification_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center"
    tools:context="pdm.isel.yawa.NotificationSettingsActivity">

    <TextView
        android:id="@+id/NotificationsTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Notifications On"/>

    <CheckBox
        android:layout_toRightOf="@id/NotificationsTextView"
        android:id="@+id/NotificationsCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <EditText
        android:layout_below="@id/NotificationsCheckBox"
        android:id="@+id/HourSelectionBox"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:textColor="@android:color/white" />

    <TimePicker
        android:layout_below="@id/NotificationsTextView"
        android:id="@+id/NotificationsTimePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        android:shadowColor="@color/colorPrimary"
        android:textColor="@android:color/white"
        android:textSize="30dp"/>

    </TimePicker>

    <Button
        android:layout_below="@+id/NotificationsTimePicker"
        android:id="@+id/DefineTimeButton"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:text="set" />

</RelativeLayout>
