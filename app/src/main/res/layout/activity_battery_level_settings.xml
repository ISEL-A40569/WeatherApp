<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_battery_level_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="top"
    tools:context="pdm.isel.yawa.BatteryLevelSettingsActivity">

    <TextView
        android:id="@+id/BatteryLevelTextView"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:shadowColor="@color/colorPrimary"
        android:textColor="@android:color/white"
        android:textSize="25dp" />
    android:textSize="80dp"/>

    <SeekBar
        android:id="@+id/BatteryLevelBar"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_below="@id/BatteryLevelTextView"
        android:max="100" />

    <Button
        android:layout_below="@+id/BatteryLevelBar"
        android:id="@+id/BatteryLevelButton"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:text="Set" />

</RelativeLayout>
